spring:
  application:
    name: open-blog
  cloud:
    config:
      discovery:
        enabled: true
        service-id: whale-cloud-config
      label: master
      uri: http://localhost:8770/
      profile: test
      username: root
      password: tiger
    bus:
      trace:
        enabled: true
      enabled: true
  rabbitmq:
    host: localhost
    port: 5672
    username: root
    password: tiger
eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://peer:tiger@127.0.0.1:8760/eureka/
  instance:
    prefer-ip-address: true
management:
  endpoints:
    web:
      exposure:
        include: "*"

#  curl  -X POST http://127.0.0.1:3154/actuator/bus-refresh